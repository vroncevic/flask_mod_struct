name: TOC Generator flask_func_struct
on:
  push:
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: TOC Generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v2
